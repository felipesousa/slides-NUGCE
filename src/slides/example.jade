section
  section(data-background="#1976d2")
    h3 Como começar?
    pre
      code.
        $ npm install hexo-cli -g
        $ hexo init "Name"
        $ cd Name/
        $ [sudo] npm install
        $ hexo server
  section(data-background-image="img/hexo.png")
  section(data-background="#1976d2")
    h3 Deploy
    pre
      code.
        $ hexo generate
        $ hexo deploy
